# Personal data — NEVER commit. These contain your PR titles and repo names.
data.js
cache.json
cache.json.tmp

# Scheduler logs written by the auto-refresh agent
agent.log
agent.err.log

# Local settings (notification on/off) — machine-specific
config.json
config.json.tmp

# Local tooling / editor
.claude/
.DS_Store
*.swp

# Python
__pycache__/
*.pyc
.venv/
venv/

# Local code-review tooling cache
.code-review-graph/
